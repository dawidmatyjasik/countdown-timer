import React from "react";
import { HeaderContainer, HeaderTitle } from "./HeaderElements";

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>We're launching soon</HeaderTitle>
    </HeaderContainer>
  );
};

export default Header;
